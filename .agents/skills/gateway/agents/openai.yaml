interface:
  display_name: "Gateway"
  short_description: "Repo-specific patterns and workflows for gateway"
  default_prompt: "Use the gateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true